/**
 * 通用 LLM 集成导出
 */

export { ContextBuilder } from './context-builder';
export type { Message, MessageRole } from './context-builder';
export type {
  LLMClient,
  LLMMessage,
  LLMMessageRole,
  LLMRequestOptions,
  LLMResponse,
  StreamCallback,
} from './types';
